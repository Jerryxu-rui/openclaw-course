# 第13课：企业级部署与最佳实践

## 课程目标
- 掌握企业级部署方案
- 理解高可用架构
- 学会成本控制
- 建立团队协作机制

---

## 第一部分：部署架构（30分钟）

### 1.1 单机部署

```
适用场景：
- 个人使用
- 小团队（<10人）
- 开发测试环境

架构：
[用户] → [Gateway] → [AI模型]
```

### 1.2 Docker部署

Docker部署适合需要环境隔离、方便迁移、或在服务器上长期运行的场景。

#### docker-compose快速启动
OpenClaw仓库内置了`docker-compose.yml`，一条命令即可启动：

```bash
# 克隆仓库
git clone https://github.com/openclaw/openclaw.git
cd openclaw

# 启动
docker-compose up -d
```

#### 镜像变体（Image Variants）

| 变体 | 说明 | 适用场景 |
|------|------|----------|
| 标准镜像 | 完整功能，包含所有扩展依赖 | 一般使用，功能全 |
| slim变体 | 多阶段构建，体积更小 | 资源受限环境，CI/CD |
| sandbox | 沙箱环境（Dockerfile.sandbox） | 安全隔离，代码执行 |
| sandbox-browser | 含浏览器的沙箱 | 需要浏览器自动化 |

**使用slim变体**：在`docker-compose.yml`中设置环境变量`OPENCLAW_VARIANT=slim`。v2026.3.7起支持扩展依赖预烘焙，容器镜像可预装扩展依赖，减少启动时的安装等待。

#### 挂载目录（Volume Mounts）
Docker部署需要挂载两个关键目录，确保数据持久化：

```yaml
volumes:
  - ~/.openclaw:/root/.openclaw    # 配置和状态数据
  - ~/openclaw/workspace:/workspace # 工作空间（YAML配置文件）
```

**重要**：不挂载这两个目录，容器重启后所有配置和对话记录都会丢失。`~/.openclaw`存放运行状态，`workspace`存放YAML配置文件。

#### 端口映射（Port Mapping）
OpenClaw Gateway默认监听18789端口（WebSocket），Web UI默认使用3000端口。在`docker-compose.yml`中配置端口映射：

```yaml
ports:
  - "18789:18789"  # Gateway WebSocket
  - "3000:3000"    # Web UI
```

#### Podman兼容（Podman Support）
OpenClaw同样支持Podman运行。Podman是Docker的无守护进程替代方案，命令基本兼容：

```bash
# 使用Podman启动
podman-compose up -d
```

对于需要rootless容器运行的环境（如企业安全策略要求），Podman是更合适的选择。

### 1.3 云厂商一键部署

这是大多数国内用户的首选方案。所有主流云厂商都已支持OpenClaw一键部署，差异主要在价格策略和IM生态集成上。

#### 阿里云（Alibaba Cloud）
国内社区资源最丰富的平台，镜像预装，开箱即用。

| 项目 | 详情 |
|------|------|
| 配置 | 2vCPU + 2GiB 内存 + 40GiB ESSD 系统盘 |
| 系统 | Alibaba Cloud Linux 3.2104 LTS 64位，预装OpenClaw镜像 |
| 价格 | 限时秒杀 9.9元/月，包年常规优惠低至68元/年 |
| 模型 | 默认内置qwen3.5-plus；百炼Coding Plan Lite首月10元（18,000次/月） |
| IM支持 | 钉钉、飞书等（通过openclaw-china插件） |

**部署步骤**：
1. **一键购买**：进入活动页，购买预装OpenClaw镜像的轻量应用服务器。镜像版本OpenClaw 2026.2.26。
2. **放通端口+配置**：在安全组中放通18789（Gateway）和3000（Web UI）端口，配置百炼API Key。
3. **访问Web UI**：浏览器访问`http://你的IP:3000`，进入OpenClaw管理界面，可选集成钉钉/飞书等IM。

**注意**：9.9元/月是限时秒杀价，需要抢。常规价不算最便宜，且续费价格比新购高不少。如果你不急，可以等下一波活动。

#### 腾讯云（Tencent Cloud）
四大IM全面支持，Coding Plan模型套餐性价比高。

| 项目 | 详情 |
|------|------|
| 配置 | 推荐2核4G（黄金配置），最低2核2G可运行 |
| 价格 | 新人包2核4G约17元/月，一年99元起 |
| 模型 | Coding Plan首月7.9元起，含HY 2.0 Instruct、GLM-5、kimi-k2.5、MiniMax-M2.5等 |
| IM支持 | 企微、QQ、钉钉、飞书（四大IM全覆盖） |
| 续费 | 支持「限时同价续费」活动，避免续费刺客 |

**部署步骤**：
1. **购买Lighthouse实例**：在腾讯云轻量应用服务器页面购买实例。
2. **选择OpenClaw模板**：应用模板 → AI智能体 → OpenClaw，一键安装。
3. **配置模型+接入IM**：购买Coding Plan获取模型调用能力，然后接入企微/QQ/飞书/钉钉。

#### 百度智能云（Baidu Cloud）
试错成本最低：0.01元首月体验，全图形界面操作。

| 项目 | 详情 |
|------|------|
| 配置 | 2核4G通用型，系统盘40GiB |
| 价格 | 首月0.01元体验，续费约25元/月 |
| 模型 | 文心一言4.0 Turbo免费额度，百炼API Key自动配置 |
| IM支持 | 飞书、钉钉、企微（通过openclaw-china插件） |
| 特点 | 全图形界面操作，适合非技术用户 |

**部署步骤**：
1. **选择OpenClaw模板**：在BCC实例创建页面选择「OpenClaw AI助手」模板。
2. **支付0.01元**：完成支付，等待实例创建完成（约3-5分钟）。
3. **图形界面配置**：通过Web UI完成模型配置和IM接入，无需命令行操作。

#### 选择建议
1. **新手入门**：百度智能云（0.01元试错成本低）
2. **企业用户**：腾讯云（四大IM全面支持）
3. **社区用户**：阿里云（社区资源最丰富）
4. **技术用户**：Docker部署（灵活可控）

### 1.4 分布式部署

```
适用场景：
- 中大型团队（>10人）
- 生产环境
- 高可用要求

架构：
[负载均衡]
  ├─ Gateway1
  ├─ Gateway2
  └─ Gateway3
```

### 1.5 高可用方案

```
组件：
- 负载均衡（Nginx/HAProxy）
- 多Gateway实例
- 共享存储（NFS/S3）
- 数据库集群（PostgreSQL）
```

---

## 第二部分：监控与运维（40分钟）

### 2.1 监控体系

```
监控层级：
1. 基础设施（CPU/内存/磁盘）
2. 应用层（Gateway/Agent）
3. 业务层（对话/Token/成本）
```

### 2.2 日志管理

```
日志类型：
- 访问日志
- 错误日志
- 审计日志
- 性能日志

工具：
- ELK Stack
- Grafana Loki
- Splunk
```

### 2.3 告警机制

```
告警级别：
- P0：紧急（立即处理）
- P1：重要（1小时内）
- P2：一般（当天处理）
- P3：提示（可延后）

通知渠道：
- 电话
- 短信
- 邮件
- Telegram
```

---

## 第三部分：成本控制（40分钟）

### 3.1 成本分析

```
成本构成：
1. API调用（70%）
2. 服务器（20%）
3. 存储（5%）
4. 带宽（5%）
```

### 3.2 优化策略

```
策略1：模型选择
- 日常任务用便宜模型
- 复杂任务用贵模型
- 智能路由

策略2：缓存
- 重复查询缓存
- 上下文缓存
- 结果缓存

策略3：压缩
- 上下文压缩
- 记忆蒸馏
- 数据压缩
```

### 3.3 预算管理

```
预算分配：
- 开发环境：10%
- 测试环境：20%
- 生产环境：70%

监控：
- 实时成本追踪
- 预算告警
- 成本报告
```

---

## 第四部分：团队协作（30分钟）

### 4.1 多用户管理

```
角色：
- 管理员：全部权限
- 开发者：开发权限
- 用户：使用权限

权限控制：
- 基于角色（RBAC）
- 基于资源
- 基于时间
```

### 4.2 知识共享

```
共享内容：
- Skills
- 配置模板
- 最佳实践
- 故障案例

工具：
- 飞书知识库
- Confluence
- GitHub Wiki
```

---

## 第五部分：最佳实践（30分钟）

### 5.1 开发规范

```
代码规范：
- 统一配置格式
- 统一命名规范
- 统一错误处理

文档规范：
- README必备
- 配置说明
- 故障排查
```

### 5.2 运维规范

```
变更管理：
- 变更申请
- 变更审批
- 变更执行
- 变更验证

应急响应：
- 故障分级
- 响应流程
- 恢复方案
- 事后总结
```

---

## 作业与练习

### 作业1：部署方案设计（必做）
设计一个企业级部署方案

### 作业2：监控体系搭建（必做）
搭建基础监控体系

### 作业3：成本优化（选做）
制定成本优化方案

---

## 课程总结

### 本节课你学到了：
✅ 企业级部署架构  
✅ 监控与运维体系  
✅ 成本控制策略  
✅ 团队协作机制  

### 关键要点：
1. **高可用是基础**
2. **监控是保障**
3. **成本要控制**
4. **协作要规范**

---

**恭喜完成第三阶段（高级进阶）！** 🎉

**下节课见！** 🚀
